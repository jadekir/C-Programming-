# Queue Elimination Simulation

This program simulates groups of students moving through multiple queues with elimination rules.

It uses linked-list queues, dynamic memory, and structured data instead of simple arrays. This was one of my stronger C assignments because it had more moving parts and required keeping the queue state organized.

Run:

```sh
gcc -o queue_sim main.c
./queue_sim
```


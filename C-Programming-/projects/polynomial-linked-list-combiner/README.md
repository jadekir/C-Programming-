# Polynomial Linked List Combiner

This program works with polynomial terms stored in linked lists.

The assignment was mainly about creating nodes, combining lists, and keeping the data organized while using dynamic memory.

Run:

```sh
gcc -o polynomial main.c
./polynomial
```

The sample data file is included as `pa6_data.txt`.


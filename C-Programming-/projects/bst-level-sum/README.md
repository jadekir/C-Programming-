# Binary Search Tree Level Sum

This program builds a binary search tree from input data and calculates the sum of the nodes at a specific tree level.

I added this one because it shows tree traversal, recursion, dynamic allocation, and cleanup.

Run:

```sh
gcc -o bst_level_sum main.c
./bst_level_sum
```

The sample input file is included as `pa3_data.txt`.


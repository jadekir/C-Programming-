# Linked List Card Game

This was a C data structures assignment using linked lists, structs, and dynamic memory.

The project manages cards and player data without relying on fixed-size arrays for everything. It was one of the assignments where pointers started to make more sense because the program actually needed them.

Run:

```sh
gcc -o card_game main.c
./card_game
```


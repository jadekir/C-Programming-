# Jumping Frog Dynamic Programming

This program calculates the probability that a frog lands exactly on a target position when each jump can be a random length.

It is a smaller file, but I included it because it shows dynamic programming instead of just loops and conditionals.

Run:

```sh
gcc -o jumping_frog main.c
./jumping_frog
```


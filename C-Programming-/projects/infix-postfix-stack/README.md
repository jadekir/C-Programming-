# Infix to Postfix Stack Program

This program checks whether an expression has balanced parentheses and then converts infix notation to postfix notation.

It uses a stack, so it was good practice for thinking through order of operations and how parsing works at a basic level.

Run:

```sh
gcc -o expression_stack main.c
./expression_stack
```


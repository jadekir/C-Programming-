# Trie Prefix Counter

This project builds a trie from a list of words and counts prefix matches.

I used it to practice recursive structures, pointers, dynamic allocation, and freeing memory after the tree is done being used.

Run:

```sh
gcc -o trie main.c
./trie
```

The sample data file is included as `pa5_data.txt`.


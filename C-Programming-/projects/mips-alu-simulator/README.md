# MIPS ALU Simulator

This project implements parts of a MIPS-style simulator in C.

The main focus is ALU operations, instruction decoding, memory/register behavior, and how a processor handles low-level instructions.

Run:

```sh
gcc -o mips_sim project.c spimcore.c
./mips_sim
```

